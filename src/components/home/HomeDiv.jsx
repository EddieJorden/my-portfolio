import styled from 'styled-components';

export const HomeDiv = styled.div`
    object-fit: cover;
    overflow: hidden;

    img {
        object-fit: cover;
        overflow: hidden;
        max-height: 100%;
    }

    .image-div {
        object-fit: cover;
        overflow: hidden;
    }
`